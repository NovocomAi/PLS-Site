services:
  - type: web
    name: pls-crm
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    disk:
      name: pls-uploads
      mountPath: /mnt/data
      sizeGB: 10
    envVars:
      - key: NODE_ENV
        value: production
